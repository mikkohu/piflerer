export default class ElevatorButton extends Phaser.Sprite{
    constructor({game, x, y, asset, floor}) {
	super(game,x,y,asset);

	this.anchor.setTo(0.5);
	this.floor = floor;
	this.enableBody = true;
	
    }
}
